// <auto-generated />
// Generated by GodotCascade from res://examples/codegen/settings_bindings.gxml. Do not edit.
#nullable enable
using System;
using System.Globalization;
using Godot;

namespace CascadeShowcase
{
    public partial class SettingsBindings : Control
    {
        [Export] public NodePath CascadeDocumentPath { get; set; } = "CascadeDocument";
        private Control _cascadeDocument = null!;
        private bool _applyingGeneratedBindings;

        private partial string GetProfile();
        private partial void SetProfile(string value);
        private partial double GetUiScale();
        private partial void SetUiScale(double value);
        partial void OnGeneratedBindingsReady();

        private static string Percent(double value)
        {
        #line 12 "res://examples/codegen/settings_bindings.gxml"
            return $"{value:0}%";
        #line default
        }

        private static bool ParsePercent(string value, out double result)
        {
        #line 15 "res://examples/codegen/settings_bindings.gxml"
            return double.TryParse(
                value.Trim().TrimEnd('%'),
                NumberStyles.Float,
                CultureInfo.InvariantCulture,
                out result
            );
        #line default
        }

        public override void _Ready()
        {
            base._Ready();
            _cascadeDocument = GetNode<Control>(CascadeDocumentPath);
            var reloaded = Callable.From<Control>(OnCascadeDocumentReloaded);
            if (!_cascadeDocument.IsConnected("document_reloaded", reloaded))
                _cascadeDocument.Connect("document_reloaded", reloaded);
            OnGeneratedBindingsReady();
            RebindGeneratedControls();
        }

        private void OnCascadeDocumentReloaded(Control _root) => RebindGeneratedControls();

        private void RebindGeneratedControls()
        {
            var control0 = FindGeneratedControl("profile");
            if (control0 != null)
            {
                var callback = Callable.From<string>(OnGeneratedProfileText0Changed);
                if (!control0.IsConnected("text_changed", callback))
                    control0.Connect("text_changed", callback);
            }
            var control1 = FindGeneratedControl("ui-scale");
            if (control1 != null)
            {
                var callback = Callable.From<double>(OnGeneratedUiScaleValue1Changed);
                if (!control1.IsConnected("value_changed", callback))
                    control1.Connect("value_changed", callback);
            }
            var control2 = FindGeneratedControl("ui-scale-entry");
            if (control2 != null)
            {
                var callback = Callable.From<string>(OnGeneratedUiScaleEntryText2Changed);
                if (!control2.IsConnected("text_changed", callback))
                    control2.Connect("text_changed", callback);
            }
            RefreshGeneratedBindings();
        }

        public void RefreshGeneratedBindings()
        {
            _applyingGeneratedBindings = true;
            try
            {
                var bound0 = FindGeneratedControl("profile");
                if (bound0 != null)
                    bound0.Set("text", Convert.ToString(GetProfile(), CultureInfo.InvariantCulture) ?? string.Empty);
                var bound1 = FindGeneratedControl("profile-output");
                if (bound1 != null)
                    bound1.Set("text", Convert.ToString(GetProfile(), CultureInfo.InvariantCulture) ?? string.Empty);
                var bound2 = FindGeneratedControl("ui-scale");
                if (bound2 != null)
                    bound2.Set("value", GetUiScale());
                var bound3 = FindGeneratedControl("ui-scale-output");
                if (bound3 != null)
                    bound3.Set("text", Percent(GetUiScale()));
                var bound4 = FindGeneratedControl("ui-scale-entry");
                if (bound4 != null)
                    bound4.Set("text", Convert.ToString(GetUiScale(), CultureInfo.InvariantCulture) ?? string.Empty);
            }
            finally
            {
                _applyingGeneratedBindings = false;
            }
        }

        private void OnGeneratedProfileText0Changed(string value)
        {
            if (_applyingGeneratedBindings) return;
            SetProfile(value);
            RefreshGeneratedBindings();
        }

        private void OnGeneratedUiScaleValue1Changed(double value)
        {
            if (_applyingGeneratedBindings) return;
            SetUiScale(value);
            RefreshGeneratedBindings();
        }

        private void OnGeneratedUiScaleEntryText2Changed(string value)
        {
            if (_applyingGeneratedBindings) return;
            if (!ParsePercent(value, out var parsed)) return;
            SetUiScale(parsed);
            RefreshGeneratedBindings();
        }

        private Control? FindGeneratedControl(string id)
        {
            return FindGeneratedControlIn(_cascadeDocument, id);
        }

        private static Control? FindGeneratedControlIn(Node node, string id)
        {
            if (node is Control control && control.HasMeta("cascade_id") && control.GetMeta("cascade_id").AsString() == id)
                return control;
            foreach (Node child in node.GetChildren())
            {
                var found = FindGeneratedControlIn(child, id);
                if (found != null)
                    return found;
            }
            return null;
        }
    }
}
